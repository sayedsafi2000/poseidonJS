import { AccountAreaLayout } from '@/components/account/AccountAreaLayout';

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return <AccountAreaLayout>{children}</AccountAreaLayout>;
}
